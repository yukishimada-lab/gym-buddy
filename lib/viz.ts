/**
 * アプリ内で色に意味を持たせる箇所(グラフ・カレンダー・前回比較)で使う配色。
 *
 * dataviz スキルのリファレンスパレット(ライトモード)から取っている。
 * このアプリは白背景 (#ffffff のカード) 固定なので、ライトモードの値だけを持つ。
 *
 * - categorical: 「種類の違い」を表す(カレンダーのトレーニング / 食事)
 *   → スロット順は固定。使い回して順番を変えない。
 * - diverging:   「良い / 悪い」の向きを表す(前回比較の 伸び / 落ち)
 *   → 青 ↔ 赤 の対。中立はグレー。
 *
 * いずれも scripts/validate_palette.js で検証済み(色覚多様性・コントラストとも PASS)。
 * 色は必ず「矢印・ラベル・凡例」など色以外の手掛かりと併用すること。
 */
export const VIZ = {
  /** カテゴリ 1: 青(トレーニング / 体重) */
  series1: "#2a78d6",
  /** カテゴリ 2: オレンジ(食事 / 体脂肪率) */
  series2: "#eb6834",
  /** カテゴリ 3: アクア(骨格筋量) */
  series3: "#1baf7a",

  /** 前回比で伸びた(青)。白背景の小さい文字でも読めるステップ */
  up: "#256abf",
  /** 前回比で落ちた(赤)。白背景の小さい文字でも読めるステップ */
  down: "#c92c2c",

  /** 背景に敷く薄い色(面で塗るところ用)。濃い文字を載せても読める明度にしている */
  series1Tint: "#d3e3fa",
  series2Tint: "#fbe1d5",
  upTint: "#e6effc",
  downTint: "#fceaea",

  surface: "#ffffff",
  grid: "#e1e0d9",
  axis: "#c3c2b7",
  muted: "#898781",
  textPrimary: "#0b0b0b",
  textSecondary: "#52514e",
} as const;
